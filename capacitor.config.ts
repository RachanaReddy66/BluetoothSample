import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.co.visiontek.bluetoothsample',
  appName: 'BluetoothSample',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
